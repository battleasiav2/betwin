# BET369WIN

Laravel (Xaxino / sunfyre) site for Hostinger.

## Database (from previous betwin Hostinger setup)

- DB_HOST=`localhost`
- DB_DATABASE=`u811189100_betwin`
- DB_USERNAME=`u811189100_betwin`
- DB_PASSWORD= set in server `core/.env` (not stored in git)

Import your SQL into `u811189100_betwin`, then copy `core/.env.example` → `core/.env` and set the password.

Document root should point to this repo root (contains `index.php`).
